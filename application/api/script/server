#!/bin/sh

# script/server: Launch the application and any extra required processes locally.

set -e

cd "$(dirname "$0")/.."

go-wrapper download
go-wrapper install

go-wrapper run --db-connection=$DB_CONNECTION --env=$APP_ENV
